discord history dumper

just a quick script to grab channel messages and attachments and dump them into local jsonl files so I don't have to use the awful discord search.

install:
- pip install -r requirements.txt

run:
- python scrape.py --token YOUR_TOKEN --channel CHANNEL_ID

<!-- refreshed: 2026-09-12 -->
